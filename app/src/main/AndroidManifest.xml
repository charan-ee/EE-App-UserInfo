<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.userinfo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ui_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ui_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UserInfo">
        <activity
            android:name="com.android.userinfo.InfoDisplayActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:exported="true" />
        <activity
            android:name="com.android.userinfo.UserInputActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:theme="@style/Theme.UserInfo.NoActionBar"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>